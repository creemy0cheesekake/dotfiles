-- require('nvim-tree').setup(
--     view = {
--         mappings = {
--             list = {
-- 				{ key = "h", cb = tree_cb("dir_up") }, --tree_cb and the cb property are deprecated
-- 			}
--         }
--     }
-- })

require('nvim-tree').setup({})
